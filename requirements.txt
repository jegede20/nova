# --- Core (required) ---
PySide6>=6.6
httpx>=0.27

# --- Windows integration (installed on Windows only) ---
pywin32>=306 ; sys_platform == "win32"
winshell>=0.6 ; sys_platform == "win32"
comtypes>=1.4 ; sys_platform == "win32"
pyttsx3>=2.90
psutil>=5.9
pyautogui>=0.9.54
pillow>=10.0
numpy>=1.26

# --- Optional but recommended (Nova degrades gracefully without them) ---
# speech-to-text (local):        pip install faster-whisper
# UI-automation (accessibility): pip install uiautomation
# browser automation:            pip install playwright && playwright install chromium
# windows toast notifications:   pip install win11toast
