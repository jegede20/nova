# Windows batch files must use CRLF or "goto"/labels can misbehave.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Everything else: normalise to LF in the repo.
* text=auto
*.png binary
*.ico binary
*.zip binary
